# 舒尔特方格训练 — 小程序开发文档

## 1. 游戏概述

舒尔特方格（Schulte Grid）是一种经典的注意力训练工具。玩家需要在打乱的数字网格中，**按从小到大的顺序依次点击数字**（1→2→3→…→N），用时越短说明注意力越集中。

本游戏在原版舒尔特方格基础上扩展了**5种难度、5种布局模式、3种叠加效果**，共计 **8 种切换模式**。

---

## 2. 难度等级（5 级）

| 难度 | 网格尺寸 | 数字总数 | 按钮文字 |
|------|---------|---------|---------|
| 入门 | 3×3 | 9 | 入门 |
| 初级 | 4×4 | 16 | 初级 |
| 中级 | 5×5 | 25 | 中级 |
| 高级 | 6×6 | 36 | 高级 |
| 专家 | 7×7 | 49 | 专家 |

难度切换时：如果游戏正在进行中，先停止再以新难度重新开始。

---

## 3. 布局模式（5 种，互斥）

### 3.1 普通模式
- 数字按 1~N 顺序生成后随机打乱（Fisher-Yates shuffle）
- 数字显示在方形格子中，排列成 N×N 网格
- 格子背景白色 `#FFFFFF`，边框 `#E0E0E0`，圆角 `8px`
- 每个数字使用随机颜色（从 4 色调色板中选取：`#f44336` 红、`#2196F3` 蓝、`#FF9800` 橙、`#9C27B0` 紫）
- 字体大小：3×3 用 `2.5rem`，4×4 用 `2.2rem`，5×5 用 `1.8rem`，6×6 用 `1.5rem`，7×7 用 `1.2rem`
- 格子 hover 时边框变 `#667eea`，轻微放大 `scale(1.05)`
- 格子 active 时缩小 `scale(0.95)`

### 3.2 偏移模式
- 在普通模式基础上，每个数字**随机旋转一个角度**（-15° ~ +15° 之间）
- 旋转是静态的（生成时确定，不随时间变化）
- 其余与普通模式完全相同

### 3.3 混乱模式
- 在普通模式基础上，每个数字**持续动态摆动**
- CSS 动画：`chaos-rotate`，每个数字独立随机角度，持续摆动
- 关键帧：0%→25%→50%→75%→100%，在 -15°~+15° 之间摆动
- 每个数字的动画延迟随机（0~2s），摆动周期 3s
- 数字被正确点击后，摆动动画停止（`animation: none`）

### 3.4 圆形（静）模式
- 数字排列成**同心圆环**，而非方格
- 环形配置按难度不同：

| 难度 | 环数 | 每环格数 | 总格数 | 每环字体 |
|------|-----|---------|-------|---------|
| 入门(3×3) | 2层 | 4+6 | 10 | 34px |
| 初级(4×4) | 2层 | 8+12 | 20 | 32px/34px |
| 中级(5×5) | 3层 | 6+10+14 | 30 | 34px |
| 高级(6×6) | 3层 | 6+12+22 | 40 | 30px |
| 专家(7×7) | 4层 | 6+12+18+24 | 60 | 22~28px |

- 每个扇形（sector）用 SVG `<path>` 绘制，文字用 SVG `<text>`
- 扇形背景白色 `#FFFFFF`，边框 `#B0C4B8`
- 数字颜色随机（同上 4 色调色板）
- 圆形模式有**子状态切换**（见第 5 节）

### 3.5 混沌（动/转）模式
- 使用 Canvas 绘制，数字变成**弹跳小球**
- 小球在 Canvas 区域内自由弹跳（碰撞边界反弹）
- 球数 = 当前难度的总数字数（如 5×5 = 25 个球）
- 球半径：3×3=28px, 4×4=24px, 5×5=20px, 6×6=16px, 7×7=14px
- 球速：随机 0.3~0.8 px/frame（含方向）
- 球颜色：4 色调色板随机
- 球上数字颜色：白色 `#FFFFFF`
- Canvas 背景白色，尺寸 500×500（居中缩放适配）
- 三态切换按钮：**动1（交替旋转）→ 动2（7环变速）→ 关闭（回到普通模式）**

---

## 4. 叠加效果模式（3 种，可与基础模式叠加）

### 4.1 换字模式
- 将数字**替换为随机中文字符**
- 字符集（28个）：`日月山水火土金木风云雨雪天地人花鸟鱼虫春夏秋冬星雷电光心手眼`
- 每个格子随机取一个字符
- 玩家仍需按**原始数字顺序**点击（1→2→3→…），只是看到的不是数字而是汉字
- 点击正确后，该格子显示回原始数字（绿色底）
- 可叠加在任何基础模式上（普通/偏移/混乱/圆形/混沌）
- 按钮颜色：蓝色 `#2196F3`

### 4.2 虚无模式
- **方格模式**下：
  - 格子背景变透明，边框颜色变成放射动画（`ring-cycle` 颜色循环）
  - 数字颜色变白色
  - 边框有 `box-shadow` 发光效果
  - 正确点击后格子变绿色底 + 白色数字
- **圆形模式**下：
  - 扇形填充变透明，边框用 CSS 动画 `ring-cycle` 循环颜色
  - 文字变白色
  - 扇形 hover 时边框发光
  - 圆形旋转速度 +10%（`void-speed` 类）
- 按钮颜色：紫色 `#9C27B0`

### 4.3 毁灭模式
- **三态循环**：关闭 → 毁灭1 → 毁灭2 → 关闭
- **毁灭1（背景颜色变换）**：
  - 方格模式：格子背景变成棋盘格黑白交替（`#000000` / `#f2f2f2`），数字颜色与背景相反
  - 圆形模式：扇形填充棋盘格黑白交替，数字颜色与背景相反
- **毁灭2（中心炫光/12色循环）**：
  - 方格模式：格子背景用 CSS `destroy-color-cycle` 动画循环 12 种颜色
  - 圆形模式：每个扇形独立用 JS `setInterval`（680ms 间隔）循环 12 种颜色
  - 12 色：`#FF0000, #FF7F00, #FFFF00, #7FFF00, #00FF00, #00FF7F, #00FFFF, #007FFF, #0000FF, #7F00FF, #FF00FF, #FF007F`
  - 数字颜色白色
  - 正确点击后该格子/扇形锁定为绿色，不再参与颜色循环
- 按钮颜色：粉色 `#E91E63`

### 4.4 效果叠加规则
- 换字、虚无、毁灭三者**可同时叠加**
- 虚无和毁灭互斥时，后激活的覆盖前者（先关掉另一个再开新的）

---

## 5. 圆形模式子状态

### 5.1 旋转状态（三态切换）
按钮点击循环：**静 → 动1（交替旋转）→ 动2（7环变速）→ 静**

- **静**：不旋转
- **动1（交替旋转）**：奇数环 CW（顺时针），偶数环 CCW（逆时针），文字反旋转保持可读
  - 动画周期：内环 8s，外环 12s（`rotate-cw` / `rotate-ccw`）
- **动2（7环变速）**：全部 CW，内圈快外圈慢
  - 每环独立动画周期：`rotate-cw-r0` 4s → `rotate-cw-r6` 18s
  - 每 60 秒自动翻转方向（CW↔CCW），通过 `ring-reverse` 类 + `animation-direction: reverse` 实现
  - 文字同步反旋转

### 5.2 虚无+圆形
- 扇形透明，边框颜色循环动画
- 文字白色
- 旋转速度 +10%

### 5.3 毁灭+圆形
- 毁灭1：扇形棋盘格黑白
- 毁灭2：扇形 12 色循环（JS 驱动，每 680ms 切换）
- 文字白色
- 旋转速度 +10%

---

## 6. 游戏流程

### 6.1 开始游戏
1. 玩家选择难度和模式
2. 点击「开始游戏」按钮
3. 计时器开始计时（格式 `MM:SS`，每 100ms 更新）
4. 进度显示 `当前数字/总数字`（如 `5/25`）
5. 当前目标数字高亮显示在顶部

### 6.2 游戏进行中
- 玩家按顺序点击数字
- **正确点击**：
  - 格子变绿色（`#4CAF50`），数字变白色
  - 播放正确音效（440Hz，0.1s）
  - `currentNumber++`，更新进度
  - 如果是最后一个数字 → 游戏完成
- **错误点击**：
  - 格子短暂变红色（`#FF5252`），400ms 后恢复
  - 播放错误音效（200Hz，0.15s）
  - 不改变 `currentNumber`

### 6.3 游戏完成
1. 停止计时器
2. 播放完成音效（上行三音：C5→E5→G5，0.5s）
3. 显示结果面板（overlay）：
   - 总用时（`MM:SS` 格式）
   - 难度信息（如「难度: 5×5 (25个数字)」或「圆形模式 (30个数字)」）
   - 平均每个数字用时（秒，保留 2 位小数）
   - 评价等级

### 6.4 评价标准

| 难度 | 非常优秀 | 优秀 | 良好 | 继续加油 | 多加练习 |
|------|---------|------|------|---------|---------|
| 3×3 | <0.5s | <0.8s | <1.2s | <1.8s | ≥1.8s |
| 4×4 | <0.6s | <0.9s | <1.4s | <2.0s | ≥2.0s |
| 5×5 | <0.7s | <1.1s | <1.6s | <2.3s | ≥2.3s |
| 6×6 | <0.8s | <1.3s | <1.9s | <2.6s | ≥2.6s |
| 7×7 | <0.9s | <1.5s | <2.1s | <2.9s | ≥2.9s |

评价基于**平均每个数字用时**（总时间 ÷ 总数字数）。

### 6.5 重新开始
- 结果面板有「关闭」按钮关闭 overlay
- 「再玩一次」按钮：重置 `currentNumber=1`，重新生成网格，等待玩家手动点击「开始游戏」

---

## 7. 工具栏布局

```
┌─────────────────────────────────────────────┐
│  舒尔特方格训练                               │
│  按顺序点击数字，从1开始依次点击到最大数          │
├─────────────────────────────────────────────┤
│  难度： [入门] [初级] [中级] [高级] [专家]       │  ← 蓝色 #667eea
├─────────────────────────────────────────────┤
│  布局： [普通] [偏移] [混乱] [圆形(静)] [混沌]    │  ← 青色 #00897B
├─────────────────────────────────────────────┤
│  效果： [换字] [虚无] [毁灭]                    │  ← 蓝/紫/粉
├─────────────────────────────────────────────┤
│  当前目标: 5    进度: 4/25    计时: 00:03      │
├─────────────────────────────────────────────┤
│                                               │
│           ┌───┬───┬───┬───┬───┐              │
│           │ 7 │ 12│ 1 │ 19│ 3 │              │
│           ├───┼───┼───┼───┼───┤              │
│           │ 15│ 8 │ 22│ 5 │ 11│              │
│           │ …（N×N 网格）                      │
│           └───┴───┴───┴───┴───┘              │
│                                               │
├─────────────────────────────────────────────┤
│              [开始游戏]                        │
└─────────────────────────────────────────────┘
```

---

## 8. 音效系统

使用 Web Audio API（`OscillatorNode` + `GainNode`）生成简单音效：

| 音效 | 频率 | 时长 | 音量 | 说明 |
|------|------|------|------|------|
| start | C5→E5 滑音 | 0.2s | 0.3→0 | 开始游戏 |
| correct | A4 (440Hz) | 0.1s | 0.2→0 | 正确点击 |
| wrong | 200Hz | 0.15s | 0.3→0 | 错误点击 |
| complete | C5→E5→G5 | 0.5s | 0.3→0 | 游戏完成 |

小程序中可用 `wx.createInnerAudioContext()` 或振动反馈替代。

---

## 9. 自适应布局

- 方格容器最大宽度 `min(94vw, 600px)`
- 格子尺寸自适应：`(容器宽 - padding×2 - gap×(N-1)) / N`，上限 120px
- 圆形 SVG 容器 `max-width: 500px`，居中
- 混沌 Canvas 容器 `max-width: 500px`，居中
- 工具栏按钮 `flex-wrap: wrap`，小屏自动换行
- 所有尺寸使用 `clamp()` 平滑缩放

---

## 10. 状态管理

核心状态变量：

```
gridSize: 3|4|5|6|7          // 当前难度
currentMode: 'normal'|'offset'|'chaos'|'circular'|'bounce'  // 当前布局模式
currentNumber: 1~N            // 当前需要点击的数字
isPlaying: boolean            // 是否正在游戏中
startTime: timestamp          // 游戏开始时间戳
timerInterval: intervalId     // 计时器 ID

// 圆形模式
isCircularMode: boolean
circularRotating: 0|1|2      // 旋转状态
circularChaos: boolean        // 圆形混乱（已废弃）
circularDestroy: 0|1|2       // 毁灭状态

// 混沌模式
isBounceMode: boolean
bounceBalls: [{x,y,vx,vy,number,color,radius}]

// 叠加效果
fontMode: boolean             // 换字
voidMode: boolean             // 虚无
destroyMode: 0|1|2            // 毁灭状态

// 难度切换
if (isPlaying) stopGame()
startGame()
```

---

## 11. 关键交互规则

1. **难度切换**：游戏进行中切换难度 → 先停止当前游戏，再以新难度开始
2. **模式切换**：切换布局模式时，如果游戏进行中 → 停止游戏，切换到新模式
3. **圆形模式**：进入圆形模式后，难度按钮隐藏（圆形模式有自己独立的格数配置）
4. **混沌模式**：进入混沌模式后，难度按钮隐藏
5. **虚无/毁灭互斥**：开启一个时自动关闭另一个
6. **换字模式**：可独立开关，不影响其他效果
7. **开始按钮**：游戏未开始时显示「开始游戏」，进行中不显示
8. **再玩一次**：不直接开始新游戏，回到待机状态，等待玩家手动点击开始

---

## 12. 数据生成算法

### 12.1 数字生成（所有模式通用）
```
1. 生成数组 [1, 2, 3, ..., N]（N = gridSize² 或圆形模式总格数）
2. Fisher-Yates 洗牌：从末尾向前，每个位置与随机位置交换
3. 混乱模式额外洗牌一次（双层随机）
```

### 12.2 圆形模式扇形坐标
```
sectorPath(cx, cy, r1, r2, startDeg, endDeg):
  - 将角度转为弧度（减去90°使0°在顶部）
  - 外弧：从 startDeg 到 endDeg，半径 r2
  - 内弧：从 endDeg 到 startDeg，半径 r1（r1=0 时从圆心出发）
  - 返回 SVG path d 属性
```

### 12.3 混沌模式小球初始化
```
initBalls(count):
  for i in 1..count:
    x = random(radius, canvasWidth - radius)
    y = random(radius, canvasHeight - radius)
    vx = random(0.3, 0.8) * randomSign()
    vy = random(0.3, 0.8) * randomSign()
    color = random(4色调色板)
    radius = 按难度计算
```

---

## 13. 视觉规范

### 13.1 颜色
| 用途 | 颜色值 |
|------|--------|
| 页面背景 | `#F0F4F8` |
| 卡片/容器背景 | `#FFFFFF` |
| 主文字 | `#1a1a2e` |
| 次要文字 | `#666` |
| 难度按钮（默认） | `#667eea`（蓝紫） |
| 布局按钮（默认） | `#00897B`（青色） |
| 换字按钮 | `#2196F3`（蓝） |
| 虚无按钮 | `#9C27B0`（紫） |
| 毁灭按钮 | `#E91E63`（粉） |
| 正确反馈 | `#4CAF50`（绿） |
| 错误反馈 | `#FF5252`（红） |
| 数字颜色（4色调色板） | `#f44336` `#2196F3` `#FF9800` `#9C27B0` |

### 13.2 圆角
- 容器：`20px`
- 格子：`8px`
- 按钮：`8px`

### 13.3 字体
- 数字/计时器：等宽字体 `JetBrains Mono` 或 `Courier New`
- 中文：系统默认中文字体
- 格子内数字：`font-weight: 600`

### 13.4 阴影
- 容器：`0 10px 40px rgba(0,0,0,0.1)`
- 格子 hover：`0 4px 12px rgba(102,126,234,0.3)`
- 正确格子：`0 0 20px rgba(76,175,80,0.4)`

---

## 14. 小程序适配注意事项

1. **Canvas**：混沌模式使用 Canvas 2D，小程序用 `<canvas type="2d">` 
2. **SVG**：圆形模式使用 SVG，小程序不支持 SVG 标签，需改用 Canvas 绘制圆形扇形
3. **Web Audio API**：小程序不支持，改用 `wx.createInnerAudioContext()` 播放预录音频，或使用振动反馈 `wx.vibrateShort()`
4. **DOM 操作**：小程序无 DOM，需用数据驱动视图（setData）
5. **CSS 动画**：`@keyframes` 在小程序中支持有限，复杂动画建议用 `wx.createAnimation()` 或 Canvas
6. **字体**：等宽字体在小程序中可能不可用，需测试
7. **响应式**：小程序用 `rpx` 单位，`750rpx = 屏幕宽度`

---

## 15. 模式切换状态机

```
                    ┌──────────┐
         ┌─────────→│  普通     │←─────────┐
         │          └────┬─────┘          │
         │               │                │
    ┌────┴─────┐   ┌────┴─────┐   ┌──────┴───┐
    │  偏移     │   │  混乱     │   │ 混沌(动/转)│
    └──────────┘   └──────────┘   └──────────┘
                                       │
         ┌─────────────────────────────┘
         │
    ┌────┴─────────┐
    │ 圆形(静/动/变速)│
    └──────────────┘

叠加效果（可与任何布局模式组合）：
  [换字] ←→ [虚无] ←→ [毁灭]
    ↕         ↕         ↕
  独立      互斥      互斥
```

---

## 16. 文件结构建议（小程序）

```
pages/
  schulte/
    index.js          // 主逻辑
    index.wxml        // 模板
    index.wxss        // 样式
    index.json        // 配置
    canvas.js         // Canvas 绘制逻辑（混沌模式 + 圆形模式）
    audio.js          // 音效管理
utils/
  shuffle.js          // Fisher-Yates 洗牌
  constants.js        // 颜色、字体大小等常量
```

---

*文档生成日期：2026-06-27*
*基于 schulte-grid.html v1.0 完整分析*
